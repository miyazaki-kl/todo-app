# テスト手順書

このファイルは、プロジェクトベースTodoアプリケーションのE2Eテスト手順をMCP Playwrightで実行するためのガイドです。

## テスト対象

### パスワードハッシュ化機能
- ✅ bcryptjsを使用したパスワードハッシュ化（12 salt rounds）
- ✅ 認証API でのハッシュ検証
- ✅ シードスクリプトでのハッシュ化パスワード保存
- ✅ プロフィール画面でのパスワード変更機能

### ログイン機能
- ✅ admin@example.com / admin
- ✅ test@example.com / test123
- ✅ ログイン後のプロジェクト選択画面表示
- ✅ ユーザー情報表示とログアウト機能

### プロジェクト管理機能
- ✅ プロジェクト一覧表示
- ✅ プロジェクト作成・編集・削除
- ✅ プロジェクトカラーバッジ表示
- ✅ プロジェクト内Todo管理

### マイページ機能
- ✅ 担当しているTODOの表示（優先表示）
- ✅ 作成したTODOの表示（セカンダリ表示）
- ✅ プロジェクト情報・ラベル情報の表示
- ✅ TODO詳細ページへの遷移
- ✅ 完了状態の視覚的表示

## 前提条件

### Docker環境
```bash
# データベース完全リセット（ボリュームも含めて削除）
docker compose down -v

# アプリケーション再起動
docker compose up -d
```

### 依存関係確認
- bcryptjs: パスワードハッシュ化
- @types/bcryptjs: TypeScript型定義
- MCP Playwright: E2Eテスト実行

## MCP Playwright E2Eテスト手順

### 1. 初期確認
```bash
# Docker コンテナ状況確認
docker compose ps

# アプリケーション URL: http://localhost:3000
```

### 2. MCP Playwright テスト実行

#### ステップ1: ブラウザ起動とログイン画面確認
- **ツール**: `mcp__playwright__browser_navigate`
- **URL**: `http://localhost:3000`
- **期待結果**: ログイン画面が表示される（未ログイン時は自動でリダイレクト）

#### ステップ2: ページスナップショット
- **ツール**: `mcp__playwright__browser_snapshot`
- **目的**: ログイン画面の確認

#### ステップ3: Adminユーザーログインテスト
1. **メールアドレス入力**
   - **ツール**: `mcp__playwright__browser_type`
   - **要素**: email入力フィールド
   - **値**: `admin@example.com`

2. **パスワード入力**
   - **ツール**: `mcp__playwright__browser_type`
   - **要素**: password入力フィールド
   - **値**: `admin`

3. **ログインボタンクリック**
   - **ツール**: `mcp__playwright__browser_click`
   - **要素**: ログインボタン

4. **ログイン成功確認**
   - **期待結果**: プロジェクト選択ページ（`/`）にリダイレクト
   - **確認項目**: 
     - ヘッダーに「ログイン中: 管理者 (admin@example.com)」表示
     - 「プロジェクト選択」のタイトル表示
     - 「新しいプロジェクトを作成」ボタンの表示

#### ステップ4: ログアウト
- **ツール**: `mcp__playwright__browser_click`
- **要素**: ログアウトボタン
- **期待結果**: ログイン画面に戻る

#### ステップ5: プロジェクト作成・選択テスト
1. **プロジェクト作成**
   - 「新しいプロジェクトを作成」ボタンをクリック
   - プロジェクト名: "テストプロジェクト"
   - 説明: "E2Eテスト用プロジェクト"
   - カラー選択: "blue"
   - 作成ボタンをクリック

2. **プロジェクト選択**
   - 作成されたプロジェクトをクリック
   - Todo一覧ページ（`/projects/[id]/todos`）に遷移
   - ブレッドクラム: "プロジェクト → [プロジェクト名] → Todo一覧"

3. **プロジェクト編集テスト**
   - プロジェクト選択ページに戻る
   - プロジェクトの「設定」ボタンをクリック
   - プロジェクト名変更: "テストプロジェクト（編集済み）"
   - 説明変更: "編集テスト完了"
   - カラー変更: "green"
   - 更新ボタンをクリック
   - 変更内容の反映確認

4. **プロジェクト削除テスト**（注意：実際のテストでは慎重に実行）
   - テスト用の新しいプロジェクトを作成
   - 編集画面で削除ボタンをクリック
   - 確認ダイアログで削除を実行
   - プロジェクト一覧から削除されていることを確認

#### ステップ6: テストユーザーログインテスト
1. **ログアウト後、テストユーザーでログイン**
   - メールアドレス入力: `test@example.com`
   - パスワード入力: `test123`
   - ログイン実行
2. **期待結果**: 「ログイン中: テストユーザー (test@example.com)」表示

#### ステップ7: Todo機能動作確認（プロジェクト内）
1. **新規Todo作成**
   - "新規作成"ボタンをクリック
   - Todo作成ページ（`/projects/[id]/todos/create`）に遷移
   - タイトル: "テスト用Todo"
   - 説明: "ラベル機能テスト"
   - ラベル選択: "緊急"と"進行中"を選択
   - 担当者選択: 現在のユーザーを選択
   - Todo作成確認

2. **Todo一覧表示確認**
   - プロジェクトのTodo一覧ページに戻る
   - 作成したTodoが一覧に表示される
   - ラベルバッジが正しく表示される（緊急=赤色、進行中=青色）
   - 「👤 担当中」バッジの表示確認（担当者が現在ユーザーの場合）
   - ステータスバッジとラベルバッジの併用表示確認
   - 青色の背景（担当中の場合）

3. **Todo詳細画面確認**
   - Todoアイテムをクリックして詳細画面（`/projects/[id]/todos/[todoId]`）に遷移
   - タイトル、説明、ラベルが正しく表示される
   - プロジェクト情報の表示確認（プロジェクトバッジ）
   - 作成者、担当者、作成日時、更新日時の表示確認
   - ラベルセクションに選択したラベルが表示される
   - ブレッドクラム: "プロジェクト → [プロジェクト名] → Todo一覧 → [Todo詳細]"

4. **Todo編集機能確認**
   - 詳細画面で編集ボタンをクリック
   - ラベル選択の変更（"レビュー"を追加、"緊急"を削除）
   - ステータス変更（完了に変更）
   - 更新後の詳細画面でラベルとステータスの変更確認

5. **一覧画面での変更確認**
   - 一覧に戻ってラベルの変更確認
   - 完了バッジ（✅ 完了）の表示確認

#### ステップ8: プロフィール設定・パスワード変更テスト
1. **プロフィール画面アクセス**
   - ヘッダーの「プロフィール設定」ボタンをクリック
   - プロフィールページ（`/profile`）に遷移
   - ユーザー情報セクションでメール・名前の表示確認

2. **パスワード変更機能テスト**
   - 現在のパスワード: `test123`
   - 新しいパスワード: `newpassword123`
   - 確認用パスワード: `newpassword123`
   - パスワード強度表示の確認
   - 変更ボタンをクリック
   - 成功メッセージの表示確認

3. **新パスワードでのログインテスト**
   - ログアウト
   - test@example.com / newpassword123 でログイン
   - ログイン成功確認

4. **旧パスワードでのログイン失敗テスト**
   - ログアウト
   - test@example.com / test123 でログイン試行
   - エラーメッセージ表示確認

#### ステップ9: マイページ機能テスト
1. **マイページアクセス**
   - ヘッダーの「マイページ」ボタンをクリック
   - マイページ（`/mypage`）に遷移
   - ページタイトル「マイページ」の表示確認
   - サブタイトル「{ユーザー名}さんのTODO一覧」の表示確認

2. **担当しているTODO表示確認（優先表示）**
   - 「担当しているTODO」セクションの表示確認
   - TODO件数の表示確認
   - 担当TODOがある場合：
     - TODOタイトルの表示
     - プロジェクトバッジの表示
     - ラベルバッジの表示（カラーコード付き）
     - 完了状態の視覚的表示
     - 作成日、作成者、担当者情報の表示
   - 担当TODOがない場合：
     - 「現在担当しているTODOはありません。」メッセージの表示

3. **作成したTODO表示確認（セカンダリ表示）**
   - 「作成したTODO」セクションの表示確認
   - TODO件数の表示確認
   - 重複排除の確認（担当かつ作成のTODOは担当側のみ表示）
   - 作成TODOがある場合：
     - TODOタイトルの表示
     - プロジェクト情報の表示
     - ラベル情報の表示
   - 作成TODOがない場合：
     - 「作成したTODOはありません。」メッセージの表示

4. **TODO詳細への遷移テスト**
   - マイページのTODOアイテムをクリック
   - 正しいURL（`/projects/{projectId}/todos/{todoId}`）へのリダイレクト
   - TODO詳細画面の表示確認

5. **ナビゲーション機能確認**
   - 「プロジェクト一覧に戻る」ボタンの動作確認
   - プロジェクト選択ページ（`/`）への遷移確認

#### ステップ10: 無効なログインテスト
1. **無効なパスワードでログイン試行**
   - メール: `admin@example.com`
   - パスワード: `wrongpassword`
   - **期待結果**: エラーメッセージ表示

2. **存在しないユーザーでログイン試行**
   - メール: `nonexistent@example.com`
   - パスワード: `password`
   - **期待結果**: エラーメッセージ表示

## データベース検証

### データベーススキーマ確認
```bash
# Docker PostgreSQL接続
docker compose exec db psql -U postgres -d todo_app

# テーブル一覧確認
\dt
# 期待結果: User, Todo, Label, Project, TodoLabel, _prisma_migrations テーブル

# ユーザーテーブル確認
SELECT id, email, name, LEFT(password, 10) as password_hash FROM "User";
# 期待結果: passwordカラムが$2a$12$で始まるハッシュ値

# プロジェクトテーブル確認
SELECT id, name, description, color, "createdById" FROM "Project";
# 期待結果: 作成されたプロジェクトデータ

# Todo-Project関連確認
SELECT t.id, t.title, t."projectId", p.name as project_name FROM "Todo" t
JOIN "Project" p ON t."projectId" = p.id;
# 期待結果: TodoとProjectの正しい関連付け
```

## 検証チェックリスト

### 機能テスト
- [ ] Docker環境でのアプリケーション起動
- [ ] ハッシュ化パスワードでのDB保存確認
- [ ] adminユーザーログイン成功
- [ ] testユーザーログイン成功
- [ ] ログイン後プロジェクト選択画面の表示
- [ ] ログイン後ヘッダーにユーザー情報表示
- [ ] プロジェクト作成・編集・削除機能動作
- [ ] プロジェクト選択とTodo一覧への遷移
- [ ] プロジェクト内でのTodo作成・編集・削除機能動作
- [ ] ラベル機能とバッジ表示
- [ ] ユーザー担当機能と担当中バッジ表示
- [ ] プロフィール画面でのユーザー情報表示
- [ ] パスワード変更機能が正常に動作する
- [ ] パスワード変更後、新パスワードでログインできる
- [ ] パスワード変更後、旧パスワードではログインできない
- [ ] マイページ機能の動作確認
- [ ] 担当しているTODOの表示（優先表示）
- [ ] 作成したTODOの表示（セカンダリ表示）
- [ ] マイページからTODO詳細への遷移
- [ ] ログアウト機能動作
- [ ] 無効なパスワードでのログイン拒否
- [ ] 存在しないユーザーでのログイン拒否

### セキュリティテスト
- [ ] パスワードが平文でログに出力されない
- [ ] データベースにハッシュ化パスワードが保存
- [ ] bcrypt検証での適切なタイミング攻撃対策

### パフォーマンステスト
- [ ] ログイン処理が2秒以内に完了
- [ ] パスワードハッシュ化が適切な時間で完了

## トラブルシューティング

### よくある問題

#### 1. bcryptjsモジュールエラー
```bash
# 解決策: Docker再ビルド
docker compose down -v
docker compose build --no-cache
docker compose up -d
```

#### 2. データベース接続エラー
```bash
# 解決策: コンテナとボリュームを削除して再作成
docker compose down -v
docker compose up -d
```

#### 3. ログインエラー
- **解決策**: データベース完全リセット（`docker compose down -v`）

## 次回実行時の注意点

1. **データベース状態確認**: 必ずハッシュ化パスワードが保存されていることを確認
2. **Docker環境**: 依存関係の更新でコンテナ再ビルドが必要
3. **テストデータ**: admin/testユーザーの初期パスワードを把握
4. **プロジェクト作成**: テスト用プロジェクトが必要（または既存プロジェクトの確認）
5. **MCP Playwright**: スナップショット機能で画面確認を併用
6. **ナビゲーション**: プロジェクトベースのURL構造に注意

## アーキテクチャの特徴

### プロジェクトベース設計
- 全てのTodoはプロジェクトに紐づく
- ログイン後は必ずプロジェクト選択から開始
- URL構造: `/projects/[id]/todos/[todoId]`

### ユーザー管理
- ローカルストレージベースの認証
- プロフィール画面での情報・パスワード管理
- ユーザーの担当者機能

### ラベルシステム
- カラーコード化された定義済みラベル
- Todo複数ラベル対応
- バッジによる視覚的表示

### API エンドポイント
- `GET /api/todos?projectId=[id]&currentUserId=[id]` - プロジェクト内ラベル付きTodo一覧取得
- `GET /api/todos?assignedToId=[id]` - 担当者指定でのTodo一覧取得（マイページ用）
- `GET /api/todos?createdById=[id]` - 作成者指定でのTodo一覧取得（マイページ用）
- `POST /api/todos` - ラベル割り当て付きTodo新規作成
- `GET /api/todos/[id]` - ラベル付き特定Todo取得
- `PUT /api/todos/[id]` - ラベル再割り当て付きTodo更新
- `GET /api/projects` - プロジェクト一覧取得
- `POST /api/projects` - プロジェクト新規作成
- `GET /api/projects/[id]` - 特定プロジェクト取得
- `PUT /api/projects/[id]` - プロジェクト更新
- `DELETE /api/projects/[id]` - プロジェクト削除
- `GET /api/labels` - 利用可能なラベル一覧取得
- `POST /api/auth/login` - ユーザー認証
- `PUT /api/users/password` - パスワード変更

## 成功基準

全ての検証チェックリストが ✅ になり、MCP PlaywrightでのE2Eテストが正常に完了すること。
プロジェクトベースのワークフロー全体が適切に機能し、マイページ機能により個人のTODO管理が効率化されることを確認。

### マイページ機能の成功基準
- 担当TODOが優先的に表示される
- 作成TODOがセカンダリ表示される  
- 重複TODOが正しく排除される
- プロジェクト情報・ラベル情報が正しく表示される
- TODO詳細への遷移が正常に動作する

---