# 開発ガイドライン

## 必須遵守事項

以下の設定は必ず守ってください。これらは開発環境の安定性と一貫性を保つために重要です。

### 1. パーミッション設定

パーミッション問題が発生した場合、**必ず**以下のコマンドを実行してください：

```bash
# ディレクトリのパーミッションを755に設定
sudo chmod -R 755 prisma/
sudo chmod -R 755 node_modules/
sudo chmod -R 755 .next/
```

### 2. Docker環境の設定

#### ファイル共有の原則（必須）
- ✅ `node_modules`と`.next`ディレクトリは**必ず**Dockerコンテナ内で管理
- ✅ その他のファイルは**必ず**ホストマシンとコンテナで共有（volume）

#### 推奨される設定（必須）
```yaml
volumes:
  - .:/app                    # ソースコードの共有（必須）
  - /app/node_modules         # node_modulesはコンテナ内で管理（必須）
  - /app/.next               # .nextはコンテナ内で管理（必須）
```

### 3. 設定の目的と効果

この設定により、以下の効果が**必ず**得られます：
- ホットリロードが正常に動作
- パフォーマンスの最適化
- 依存関係の分離

## 注意事項

- ❌ `node_modules`や`.next`ディレクトリをホストマシンで直接操作しないでください
- ❌ Dockerfileで`node_modules`や`.next`ディレクトリをコピーしないでください
- ✅ パーミッション問題が発生した場合は、必ず上記のコマンドを実行してください
